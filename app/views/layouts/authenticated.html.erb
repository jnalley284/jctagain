<!DOCTYPE html>
<!--[if IE 8]><html class="no-js lt-ie9" lang="en"> <![endif]-->
<!--[if gt IE 8]><!--><html class="no-js" lang="en"> <!--<![endif]-->

<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Just Call tony</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= csrf_meta_tags %>

  <script type="text/javascript" src="//use.typekit.net/jjq8hxz.js"></script>
  <script type="text/javascript">try{Typekit.load();}catch(e){}</script>
  <script type="text/javascript" src="https://js.stripe.com/v2/"></script>
</head>
<body class="application">
  <section class="app-nav-wrapper">
    <div class="row">
      <div class="large-12">
        <span class="white-logo">&nbsp;</span>
        <nav>
          <% unless current_user.first_name.blank? %>
            <span>Logged in as <%= current_user.full_name %></span>
            <span class="divider">&nbsp;</span>
          <% end %>
          <span>
            <%= link_to ""'Logout', destroy_user_session_path, method: :delete %>
          </span>
        </nav>
      </div>
    </div>
  </section>

  <% flash.each do |name, msg| %>
    <% if msg.is_a?(String) && msg.length > 0 %>
      <div data-alert class="alert-box <%= name == :notice ? "success" : "alert" %>">
        <a href="#" class="close">&times;</a>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      </div>
    <% end %>
  <% end %>

  <%= yield %>

  <section class="app-footer">
    <div class="row">
      <div class="large-12">
        <span class="help">
          If you need any assistance with this site, please call 800-818-4563 or email
          <a href="mailto:info@justcalltony.com?Subject=JCT%20Assistance">
            info@justcalltony.com
          </a>
        </span>
      </div>
    </div>
  </section>

  <%= javascript_include_tag "application" %>
</body>
</html>